# Environment and editor state
.env
.venv/
.idea/
.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.cache/
coverage.xml
htmlcov/

# JavaScript
node_modules/
.next/
out/

# Local data, databases, model artifacts, and service volumes
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep
artifacts/*
!artifacts/.gitkeep
!artifacts/pitch/
# Committed serving models (README: pre-trained artifacts are committed).
# A fresh clone must be able to run `make api` and see real metrics.
!artifacts/models/
!artifacts/models/baseline-online-xgb/
!artifacts/models/baseline-online-v3/
# bulky per-run analysis outputs are not needed to serve — keep them local
artifacts/models/*/*.parquet
volumes/
mlruns/
phoenix/

# Secrets and downloaded archives
*.zip
kaggle.json
work/
